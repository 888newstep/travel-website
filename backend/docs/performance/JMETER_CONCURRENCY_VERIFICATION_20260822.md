# JMeter 并发一致性验收记录

## 验证环境

- Windows 11 本地 MySQL 8、Redis 7、JMeter
- 测试服务使用脚本自动启动隔离端口和临时 Redis
- 每个场景结束后自动清理临时用户、业务记录、服务进程和 Redis
- 本记录不代表生产容量承诺，只用于验证并发正确性和资源清理

## 评论点赞并发

日志：`run-logs/jmeter-comment-like/20260822-174629-914/`

- 并发线程：100
- JMeter 样本：100，失败：0
- 平均响应：`1428.52 ms`
- P95/P99：`1825/1863 ms`
- 吞吐：`53.13 req/s`
- `route_comment.likes_count`：100
- 点赞行为记录：100
- 不同用户数：100
- 越路线评论数：0
- 统计接口与数据库聚合结果一致

## 景点评价 Upsert 并发

日志：`run-logs/jmeter-review/20260822-174806-394/`

- 并发线程：100
- JMeter 样本：100，失败：0
- 平均响应：`1403.54 ms`
- P95/P99：`1580/1595 ms`
- 最终 HTTP 状态：200
- 同一用户、同一景点评价最终记录数：1
- 最终评价内容为最后一次有效提交，说明 Upsert 语义成立

## 路线优化并发

日志：`run-logs/jmeter-route-optimization/20260822-174920-398/`

- 并发线程：100
- HTTP 200：100，失败：0
- 平均响应：`1929.34 ms`
- P95/P99：`2303/2334 ms`
- 路线景点关系记录：3
- 非法顺序数：0
- 优化历史记录：1
- 结果顺序与预期一致：`186,184,185`
- 临时服务端口、Redis 端口和业务残留：0

## 网关层同键幂等

网关编排日志：`run-logs/jmeter-gateway-idempotency/20260822-185659-461/`

JMeter 报告：`run-logs/jmeter/20260822-185802-498/`

- 隔离组件：Redis `16380`、user-service `18091`、route-service `18093`、collection-service `18094`
- 网关：`8090`，使用直连测试路由，不依赖 Nacos
- 并发线程：100
- 原始 HTTP 200：1
- 处理中 HTTP 409：99
- 非预期响应和失败样本：0
- 平均响应：`627.52 ms`
- P95/P99：`644/669 ms`
- 吞吐：`51.23 req/s`
- 收尾重放和最终收藏状态断言通过
- MySQL 最终收藏记录：1

## Redis 中断恢复

日志：`run-logs/redis-outage/20260822-184544-146/`

- Redis 中断期间写请求返回 HTTP 503
- 中断前后业务记录均为 1，没有绕过幂等入口写库
- Redis 重启后服务恢复，查询返回 HTTP 200
- 独立重复插入被 MySQL 1062 唯一键拒绝
- 隔离 Redis 和临时服务端口已清理

## RabbitMQ 消息并发

日志：`run-logs/rabbitmq/20260822-205241/`

- 唯一消息：100
- 数据库落库：100
- 消费耗时：`696 ms`
- 消费吞吐：`143.68 msg/s`
- 同一轮同时通过重复幂等、retry/DLQ、修正后回放、消费者重启和强制断连恢复

## 路线访问分析集成验收

日志：`run-logs/route-visit/20260822-213119-067/`

- 认证访问 3 次、匿名访问 2 次
- `view_count` 原子增加 5，访问明细新增 5 条，UV 为 3
- 访问者标识均为 64 位加盐 SHA-256，表中无原始 IP/User-Agent 列
- 所有者统计接口返回 PV/UV 与数据库一致
- 测试结束后访问明细和浏览量均恢复到基线

## 结论

当前 HTTP 幂等、Redis fail-closed、数据库唯一键、原子计数、评价 Upsert 和路线优化并发控制均已通过真实服务验收。上述结果用于证明并发正确性，不代表生产容量上限。
